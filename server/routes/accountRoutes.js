const express=require("express");const protect=require("../middleware/authMiddleware");const c=require("../controllers/accountController");const r=express.Router();r.get("/",protect,c.listAccounts);r.get("/:id/transactions",protect,c.accountTransactions);r.post("/",protect,c.createAccount);r.put("/:id",protect,c.updateAccount);r.delete("/:id",protect,c.deleteAccount);module.exports=r;
