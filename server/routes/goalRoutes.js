const express=require("express");const protect=require("../middleware/authMiddleware");const c=require("../controllers/featureController");const r=express.Router();r.get("/",protect,c.goal.list);r.get("/:id",protect,c.goal.get);r.post("/",protect,c.goal.create);r.post("/:id/contribute",protect,c.contribute);r.put("/:id",protect,c.goal.update);r.delete("/:id",protect,c.goal.remove);module.exports=r;
